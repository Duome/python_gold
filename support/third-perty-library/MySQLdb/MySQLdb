mysql中操作数据库：
----------------------------------------------------------------------------------
^^^打开mysql

   在cmd中cd 到mysql所在盘的bin
   cd bin mysql -u root -p 123456    （有密码时）
   cd bin mysql -u root       （无密码时）
----------------------------------------------------------------------------------
^^^显示当前所有数据库

   show databases;
----------------------------------------------------------------------------------
^^^操作数据库

   use test;
----------------------------------------------------------------------------------
^^^查看数据库下的表

   show tables;
----------------------------------------------------------------------------------
^^^新建表student

   create table student(id varchar(10), name varchar(20), age int);
   这里的变量定义类似C语言，需要对变量规定类型
   student————列表名
   （）————括号里面跟表头
   id varchar(10)————表头第一个元素是id，类型为字符（varchar），长度为10
   name varchar(20)————表头第二个元素是name，类型为字符（varchar），长度为20
   age int————表头第三个元素是age，类型为整型（int）
----------------------------------------------------------------------------------
^^^删除表

   drop table student；
   student————列表名
----------------------------------------------------------------------------------
^^^添加表数据

   insert student value('001', 'xiaofan', 18);
   数据为一行一行的加入
   insert————插入
----------------------------------------------------------------------------------
^^^显示表数据

   select * from student
   * ————代表全部内容
   student————列表名
   可以查看其它列表元素数据
   eg: select id from student
----------------------------------------------------------------------------------
^^^删除表数据

   delete from student where name = 'xiaofan'
   删除元素name为'xiaofan'的数据，也可以带其它元素
   student————列表名
----------------------------------------------------------------------------------
^^^修改表数据

   update student set age = '20' where name = 'xiaohong'
   修改元素name为'xiaohong'的age为'20'
   student————列表名
   update————更新
   set————设置
----------------------------------------------------------------------------------
^^^新增数据库

   create database test;
   test————数据库名
----------------------------------------------------------------------------------
^^^设置密码

   set password = password('123456');
   设置为任意用户时，增加密码，不能登录
----------------------------------------------------------------------------------
^^^退出mysql

   quit