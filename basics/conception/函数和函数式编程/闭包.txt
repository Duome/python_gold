闭包————

    定义————
        一个函数返回了一个内部函数，该内部函数引用了外部函数的相关参数和变量，我们把该返回的内部函数称为闭包（Closure）。
    作用————
        引用了自由变量，即使生成闭包的环境已经释放，闭包仍然存在。
        闭包在运行时可以有多个实例，即使传入的参数相同，也是不同的实例
        利用闭包，我们还可以模拟类的实例。
    误区————
        内部的函数引用了变量后，内部的函数并非立刻执行，
        尽量避免在闭包中引用循环变量，或者后续会发生变化的变量。